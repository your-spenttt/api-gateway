{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "Api Gateway for SPENTTT microservices",
  "dependencies": {
    "axios": "^1.5.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.7",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "start:dev": "nodemon ./dist/index.js",
    "build:watch": "rm -rf dist/ && npx tsc --watch",
    "build": "rm -rf dist/ && npx tsc",
    "start": "node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "arvinpaundra",
  "license": "ISC"
}
